## What I learned this week
I learned the content in chapter 6, including how to read and write files in Python without using with or try statements, how to use regular expressions to match word patterns, and how to build logic step by step like in the Wordle exercise where each guess adds new constraints.

## Code I'm proud of (optional)
def check_word(word):
    excluded = 'spadclrkto'
    if uses_any(word, excluded):
        return False
    if 'e' not in word:
        return False
    if word[2] == 'e' or word[3] == 'e' or word[4] == 'e':
        return False
    if word[4] != 'm':
        return False
    return True

I am most proud of this section of code because it combines everything from the previous chapter (reusing uses_any) with new logic, and I had to carefully track which letters go where across multiple guesses.

## Challenges I faced
The Wordle exercise was tricky because I had to keep track of excluded letters, correct positions, and wrong positions across three different guesses (SPADE, CLERK, TOTEM). The regex exercise for matching pale/pales/paled/paleness/pallor but not impale also took some thinking.

## AI usage (if any)
I used AI to help me with some exercise questions. I use AI only when I spent a lot of time and still not able to figure out the answer.

## Questions going forward
None
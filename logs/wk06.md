## What I learned this week
I learned the content in chapter 7, how the same logical idea can be expressed through multiple related functions. Such as, uses_none is just not uses_any, and uses_all is just uses_only with the arguments swapped.

## Code I'm proud of (optional)
def uses_all(word, required):
    """Checks whether a word uses all required letters.

    >>> uses_all('banana', 'ban')
    True
    >>> uses_all('apple', 'api')
    False
    >>> uses_all('rhythm', 'rhy')
    True
    """
    return uses_only(required, word)

I am most proud of this secction of code because I spend a lot of time and finally figured it out.

## Challenges I faced
The excercises in chapter 7 in pretty challenging. I used AI to help solve some of the questions.

## AI usage (if any)
I used AI to help me with some excercise questions. I use AI only when I spent a lot of time and still not able to figure out the answer.

## Questions going forward
None
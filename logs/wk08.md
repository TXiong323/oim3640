## What I learned this week
I learned the content in chapters 10 and 11. Chapter 10 is about dictionaries, how they store key-value pairs, how to use them to count letters or words, and how storing results in a dictionary can make slow recursive functions much faster. Chapter 11 is about tuples, how they are immutable unlike lists, and how they can be used together with dictionaries through zip and tuple assignment.

## Code I'm proud of (optional)
pythonknown = {0:0, 1:1}

def fibonacci_memo(n):
    if n in known:
        return known[n]
    res = fibonacci_memo(n-1) + fibonacci_memo(n-2)
    known[n] = res
    return res

I am most proud of this memoized Fibonacci function because it uses a dictionary to remember results that were already computed, so the function does not have to calculate the same thing twice.

## Challenges I faced
Understanding why lists cannot be used as dictionary keys but tuples can was confusing at first. The zip function in chapter 11 was also a bit tricky, especially using it with tuple unpacking inside a for loop.

## AI usage (if any)
I used AI to help me with some exercise questions. I use AI only when I spent a lot of time and still not able to figure out the answer.

## Questions going forward
None